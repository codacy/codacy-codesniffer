When function arguments are declared in multiple lines, there must be only one argument per line and
the parenthesis must be placed together on their own line. Example:

    function (
        $longArgument,
        $longerArgument,
        $muchLongerArgument
    ) {
       // body
    };

      