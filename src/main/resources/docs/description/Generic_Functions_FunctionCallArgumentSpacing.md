In function calls there must no be spaces next to the parentheses and
there must be a space after each comma in the argument list.
Examples:

    <?php
    bar();
    $foo->bar($arg1);
    Foo::bar($arg2, $arg3);

      