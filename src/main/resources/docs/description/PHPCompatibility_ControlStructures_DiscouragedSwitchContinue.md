Detect use of `continue` in `switch` control structures.As of PHP 7.3, PHP will throw a warning when `continue` is used to target a `switch`
control structure.
The sniff takes numeric arguments used with `continue` into account.

PHP version 7.3